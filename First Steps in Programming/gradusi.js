function gradusi (input){
    const radian = Number(input[0]); 
    const degrees = (radian * 180) / Math.PI; 
    console.log(degrees); 
// taka se dobavq komentar we
} 
gradusi(["3.1416"]); 
gradusi(["6.2832"]);